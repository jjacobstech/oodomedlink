---
deployment:
  tasks:
    - export DEPLOYPATH=/home/eaqahemf/public_html
    - /bin/cp -R * $DEPLOYPATH
    - cd $DEPLOYPATH
    - composer install --no-dev --optimize-autoloader
    - npm install
    - npm run build
    - php artisan config:cache
    - php artisan route:cache
    - php artisan view:cache
    - php artisan optimize
